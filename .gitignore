# 素材の元ファイル（フォルダには残るが、公開サイトには不要なので除外）
# 実際にページで使うものは assets/ に最適化して入れてある
/*.png
/*.jpg
/*.jpeg
/*.mp4
/*.mov

# 素材の元フォルダ（重いオリジナル。assets/ に最適化済みを配置している）
/SUMMER/
/New/

# OS
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store

# エディタ / ツール
.claude/settings.local.json
.vscode/
.idea/
*.swp

# Vercel
.vercel

# 環境変数（絶対にコミットしない）
.env
.env.*

# 依存関係・ビルド成果物（将来Node.jsを使う場合）
node_modules/
dist/
.cache/
